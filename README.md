# SubmitReady PDF

A local-first React + Vite MVP for turning homework photos or screenshots into a clean, submission-ready PDF. Images are processed in the browser with no backend, accounts, or database.

## Run Locally

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite.

## Build

```bash
npm run build
```
